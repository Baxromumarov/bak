struct Data {
    age:  int64
    name: string
    fl:   float64
}

struct DebugInfo {
    label: string
    code:  int
}

impl Data as d {
    func divide_age() -> (Result<int64, string>) {
        if d.age == 0 { // when i click d. it is not showing available fields, and funcs
        
            return Err("zero division error")
        }

        return Ok(d.age / 2)
    }
}
