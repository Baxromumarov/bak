package main

// Function with multiple returns
func multiReturn(a: int, b: string) -> (int, string, bool) {
    return a * 2, b, true
}

func main() -> (void) {
    // ERROR: multiReturn expects 2, got 0
    var (x, y, z) = multiReturn()
    println(x, y, z)
    return void
}
