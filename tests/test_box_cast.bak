// EXPERIMENTAL: uses box outside frozen v0.1.
package main

struct Dummy {
    x: int
}

func main() -> (void) {
    var b: Dummy box = box Dummy{x: 10}
    // Test if we can cast box to int and back
    var p: int = int(b)
    var b2: Dummy box = Dummy

    box p
    println(b2.x)
}
