package main

import "src/compiler/typecheck/typecheck.bak" as typecheck
import "src/compiler/ast/ast.bak" as ast

func main() -> (void) {
    var tcEnv: typecheck.Environment = typecheck.NewEnv()
    // var program ast.Program = ast.Program{Statements: Vec.new()}
    // var tc typecheck.TypeChecker = typecheck.New(program)
    println("TypeChecker compilation smoke test passed")
}
