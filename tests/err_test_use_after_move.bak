// ❌ TEST 1: Use after move - should fail
package main

func consume(v: Vec<int,_>) -> (int) {
    return v.len()
}

func main() -> (void) {
    var v: Vec<int,_> = Vec.from([1,2,3])
    consume(v)
    consume(v)  // ❌ should error: use of moved value
    return void
}
