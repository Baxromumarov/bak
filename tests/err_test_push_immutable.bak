// ❌ TEST: Push on immutable Vec - should fail
package main

func main() -> (void) {
    var v: Vec<int, _> = Vec.from([1, 2, 3])

    // immutable (no 'mut')
    v.push(4)

    // ❌ ERROR: cannot call 'push' on immutable variable 'v'
    return void
}
