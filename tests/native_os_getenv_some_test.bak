package main

import "std/os"

func main() -> (int) {
    var value: Result<string, string> = os.getenv("BAK_NATIVE_GETENV_SENTINEL")

    if value.is_err() {
        return 1
    }

    if value.unwrap() == "from_test" {
        return 0
    }

    return 2
}
