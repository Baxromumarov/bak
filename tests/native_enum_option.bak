package main

func main() -> (int) {
    var a: Option<int> = Some(42)
    mut var result: int = 0
    switch a {
        case Some(x) { result = x }
        case None { result = 0 }
    }
    return result
}
